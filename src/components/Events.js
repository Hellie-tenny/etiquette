import React from 'react';

const Events = (props) => {
    return(
        <div>
            <ul>
                {/* {props.events.map(
                    event => {
                        return(
                            <li>{event}</li>
                        );
                    }
                )} */}
            </ul>
        </div>
    );
}

export default Events;